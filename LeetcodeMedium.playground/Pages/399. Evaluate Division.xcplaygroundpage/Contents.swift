class Solution {
    class Node {
        var val: String
        var neighbors: [[Node, Double]]
        init(val: String, neighbors: [[Node]] = []) {
            self.val = val
            self.neighbors = neighbors
        }
    }
    func calcEquation(_ equations: [[String]], _ values: [Double], _ queries: [[String]]) -> [Double] {
        for (i, [a,b]) in equations.enumerated() {
            
        }
    }
}
